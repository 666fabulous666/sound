[serve]
addresses = ["0.0.0.0"]   # listen on your LAN, not just localhost
port = 8080               # pick any open port
open = false

[build]
target = "synth-gui/index.html"
dist = "docs"
public_url = "/sound/"
release = true
